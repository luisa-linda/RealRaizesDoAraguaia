<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.raizesdoaraguaia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/aguasquentes"
        android:label="Raízes do Araguaia"
        android:roundIcon="@mipmap/aguasquentes"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Tela inicial (ícone do app) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Telas do app -->
        <activity android:name=".HomeActivity" android:exported="true" />
        <activity android:name=".BarraAguasActivity" android:exported="false" />
        <activity android:name=".BarraSerraActivity" android:exported="false" />
        <activity android:name=".PontalPraiaAraraActivity" android:exported="false" />
        <activity android:name=".PontalLago" android:exported="false" />
        <activity android:name=".PraiaQuartoCrescenteActivity" android:exported="false" />


    </application>
</manifest>
